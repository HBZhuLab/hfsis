% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{hfsis_control}
\alias{hfsis_control}
\title{Control adaptive HF-SIS estimation}
\usage{
hfsis_control(
  windows = c(32L, 64L, 128L),
  delta = NULL,
  confidence_index = 0,
  lepski_constant = 1,
  variance_floor = 1e-12,
  truncation = c("bipower", "fixed", "none"),
  threshold_multiplier = 4,
  threshold_exponent = 0.47,
  scales = NULL
)
}
\arguments{
\item{windows}{Strictly increasing positive integer candidate window sizes.}

\item{delta}{Observation spacing in normalized time. \code{NULL} uses \code{1 / n},
where \code{n} is the number of input rows.}

\item{confidence_index}{Nonnegative confidence index used in the simultaneous
stochastic radius.}

\item{lepski_constant}{Positive multiplier in the Lepski comparison.}

\item{variance_floor}{Positive floor used only in correlation normalization.}

\item{truncation}{One of \code{"bipower"}, \code{"fixed"}, or \code{"none"}. The last
option explicitly removes jump protection.}

\item{threshold_multiplier}{Positive multiplier for coordinate thresholds.}

\item{threshold_exponent}{Nonnegative power of \code{delta} in the thresholds.}

\item{scales}{For fixed truncation, one positive scale per augmented
coordinate \verb{(X, Y)}. Ignored otherwise.}
}
\value{
An object of class \code{hfsis_control}.
}
\description{
Creates a validated control object shared by \code{\link[=prepare_hfsis]{prepare_hfsis()}}, \code{\link[=m_sis]{m_sis()}},
and \code{\link[=pr_sisis]{pr_sisis()}}. Inputs are equally spaced increments or returns. No control
value is changed silently.
}
\examples{
hfsis_control(windows = c(16L, 32L), truncation = "none")
}
\references{
Zhu, H. \emph{Ultra-high-dimensional spot support screening in
continuous-time regression}. Manuscript.
}
\seealso{
\code{\link[=prepare_hfsis]{prepare_hfsis()}}
}
