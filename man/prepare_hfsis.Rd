% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inputs.R
\name{prepare_hfsis}
\alias{prepare_hfsis}
\title{Prepare shared adaptive spot-correlation inputs}
\usage{
prepare_hfsis(
  x,
  y,
  target_index,
  control = hfsis_control(),
  variable_names = colnames(x)
)
}
\arguments{
\item{x}{Numeric matrix or data frame of covariate increments; rows are
consecutive equally spaced observations.}

\item{y}{Numeric response-increment vector.}

\item{target_index}{Row index of the first increment in the forward window.}

\item{control}{An \code{\link[=hfsis_control]{hfsis_control()}} object.}

\item{variable_names}{Unique covariate names in deterministic tie-breaking
order.}
}
\value{
An object with classes \code{c("hfsis_inputs", "hfsis")} containing
truncated local increments, thresholds, and marginal summaries for each
candidate window.
}
\description{
Validates equally spaced increment or return data, applies coordinatewise
truncation, and computes the marginal and diagonal summaries used by the
candidate forward windows. \code{target_index} is the row of the first increment
included in every forward window.
}
\examples{
data(hf_sis_example)
dat <- hf_sis_example$data
x <- as.matrix(dat[hf_sis_example$x_names])
prepare_hfsis(x, dat$y, hf_sis_example$target_index,
  hfsis_control(hf_sis_example$windows))
}
\references{
Zhu, H. \emph{Ultra-high-dimensional spot support screening in
continuous-time regression}. Manuscript.
}
\seealso{
\code{\link[=hfsis_control]{hfsis_control()}}, \code{\link[=m_sis]{m_sis()}}, \code{\link[=pr_sisis]{pr_sisis()}}
}
