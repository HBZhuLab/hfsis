% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pr-sis.R
\name{pr_sisis}
\alias{pr_sisis}
\alias{pr_sis}
\title{Partial-residual spot iterative screening (PR-SISIS)}
\usage{
pr_sisis(
  x,
  y = NULL,
  target_index = NULL,
  model_cap = 10L,
  block_size = 2L,
  control = hfsis_control(),
  variable_names = colnames(x)
)

pr_sis(
  x,
  y = NULL,
  target_index = NULL,
  model_cap = 10L,
  block_size = 2L,
  control = hfsis_control(),
  variable_names = colnames(x)
)
}
\arguments{
\item{x}{Numeric covariate input or prepared \code{hfsis_inputs} object.}

\item{y}{Response increments for raw input; ignored for prepared input.}

\item{target_index}{First forward-window row for raw input; ignored for
prepared input.}

\item{model_cap}{Maximum number of variables added to the active set.}

\item{block_size}{Positive number of variables added at each update.}

\item{control}{\code{\link[=hfsis_control]{hfsis_control()}} for raw input.}

\item{variable_names}{Variable names in deterministic order.}
}
\value{
An object with classes \code{c("pr_sis_fit", "hfsis_fit", "hfsis")}.
A complete fit contains a full variable ranking. If an active correlation
block is singular, the ranking contains the variables selected before
termination and \code{diagnostics$complete} is \code{FALSE}.
}
\description{
Performs iterative screening with active-set adaptive window selection.
At each update, the local window is selected using the marginal correlation
vector and the correlation columns associated with the current active set.
New correlation columns are computed on demand and cached.
}
\examples{
data(hf_sis_example)
dat <- hf_sis_example$data
inputs <- prepare_hfsis(as.matrix(dat[hf_sis_example$x_names]), dat$y,
  hf_sis_example$target_index, hfsis_control(hf_sis_example$windows))
pr_sisis(inputs, model_cap = 6L, block_size = 2L)
}
\references{
Zhu, H. \emph{Ultra-high-dimensional spot support screening in
continuous-time regression}. Manuscript.
}
\seealso{
\code{\link[=m_sis]{m_sis()}}, \code{\link[=screen_path]{screen_path()}}, \code{\link[=selected_window]{selected_window()}}
}
