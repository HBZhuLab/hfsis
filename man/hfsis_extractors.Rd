% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extractors.R
\name{hfsis_extractors}
\alias{hfsis_extractors}
\alias{selected_variables}
\alias{screen_scores}
\alias{screen_ranking}
\alias{selected_window}
\alias{screen_path}
\title{Extract selected variables and screening results}
\usage{
selected_variables(object)

screen_scores(object, iteration = NULL)

screen_ranking(object, iteration = NULL)

selected_window(object, iteration = NULL)

screen_path(object)
}
\arguments{
\item{object}{A fitted \code{hfsis_fit} object.}

\item{iteration}{For PR-SISIS, an optional completed iteration. For M-SIS,
only \code{NULL} or \code{1} is admissible.}
}
\value{
\code{selected_variables()} returns a character vector.
\code{screen_scores()} returns a data frame with one row per variable.
\code{screen_ranking()} returns a ranked data frame. Complete PR-SISIS fits
provide a full ordering; after singular termination, undefined ranks are
omitted. \code{selected_window()} returns one integer. For PR-SISIS,
\code{iteration = NULL} returns the final-ranking window. \code{screen_path()}
returns a data frame describing selection over iterations.
}
\description{
These accessors return ordinary base-R objects while preserving the input
variable order used for deterministic tie breaking.
}
\references{
Zhu, H. \emph{Ultra-high-dimensional spot support screening in
continuous-time regression}. Manuscript.
}
